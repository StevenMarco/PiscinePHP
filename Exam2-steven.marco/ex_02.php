<?php
    function min_len_array($array) {
       $keys = array_keys($array);
       foreach($keys as $key => $value) {
        if(strlen()) {

        }  
        
        // var_dump($r = strlen($value_key) . ","); 
         //   print_r(explode(',', $value_key));
        }
    }


   echo min_len_array(array('de' => 'aremier', 'escsc'=>"trois", 'cifefenqs' => "zeux"));

    // return
    // clé la plus courte == valeur ! si egal première valeur !  min pour la valeur la plus courte 
    // vide == false


    // echo min($array);  // --> tableau 

// implode explode sortie et retour de tab 